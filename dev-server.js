const http=require('node:http'),fs=require('node:fs'),path=require('node:path');
const root=path.join(__dirname,'public');const headers=require('./vercel.json').headers[0].headers;
http.createServer(async(req,res)=>{for(const {key,value}of headers)res.setHeader(key,value);const url=new URL(req.url,'http://localhost');res.status=n=>{res.statusCode=n;return res;};res.json=data=>{res.setHeader('Content-Type','application/json');res.end(JSON.stringify(data));};if(url.pathname.startsWith('/api/')){const name=url.pathname.slice(5);if(!['config','register'].includes(name))return res.status(404).json({ok:false});let body='';for await(const chunk of req){body+=chunk;if(Buffer.byteLength(body)>12000)return res.status(413).json({ok:false});}try{req.body=body?JSON.parse(body):{};}catch{return res.status(400).json({ok:false});}return require('./api/'+name)(req,res);}let file;try{file=path.resolve(root,'.'+decodeURIComponent(url.pathname==='/'?'/index.html':url.pathname));}catch{res.statusCode=400;return res.end();}if(!file.startsWith(root+path.sep)){res.statusCode=404;return res.end();}fs.readFile(file,(error,data)=>{if(error){res.statusCode=404;return res.end('Not found');}res.setHeader('Content-Type',({'.html':'text/html; charset=utf-8','.css':'text/css','.js':'text/javascript','.jpg':'image/jpeg','.jpeg':'image/jpeg','.png':'image/png','.svg':'image/svg+xml'})[path.extname(file)]||'application/octet-stream');res.end(data);});}).listen(Number(process.env.PORT||4174),'127.0.0.1',()=>console.log('Preview: http://127.0.0.1:'+(process.env.PORT||4174)));
