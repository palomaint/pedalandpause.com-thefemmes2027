'use strict';
const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
function validate(p){if(!p||typeof p!=='object'||Array.isArray(p))return 'Invalid request.';if(!UUID.test(p.request_id||''))return 'Refresh the page and retry.';if(!['may','october'].includes(p.edition))return 'Choose a valid edition.';if(!['shared','ensuite','private'].includes(p.room)||(p.edition==='october'&&p.room==='ensuite'))return 'Choose a valid room preference.';if(typeof p.name!=='string'||p.name.trim().length<2||p.name.length>120)return 'Enter your full name.';if(typeof p.email!=='string'||p.email.length>254||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(p.email))return 'Enter a valid email.';for(const [key,max] of [['phone',40],['notes',1500]])if(typeof p[key]!=='string'||p[key].length>max)return 'Please check your contact details and message.';if(p.consent!==true||typeof p.bike_rental!=='boolean')return 'Please confirm the contact permission.';if(p.website)return 'Unable to accept this request.';if(typeof p.turnstile_token!=='string'||p.turnstile_token.length>2048||!p.turnstile_token)return 'Please complete the security check.';return null;}
function offers(env=process.env){const make=(id,first,standard,capacity)=>{const value=env[id.toUpperCase()+'_TIER']||'first';const tier=['first','standard','full'].includes(value)?value:'full';return {price:tier==='first'?first:standard,available:tier!=='full',capacity,tier:tier==='first'?`First 4 bookings · then €${standard.toLocaleString('en-IE')}`:tier==='standard'?'Standard allocation':'This edition is full'};};return {may:make('may',749,795,8),october:make('october',999,1099,10)};}
function ready(env=process.env){return env.REGISTRATION_ENABLED==='true'&&!!env.SUPABASE_URL&&!!env.SUPABASE_SERVICE_ROLE_KEY&&!!env.TURNSTILE_SECRET_KEY&&!!env.TURNSTILE_SITE_KEY&&!!env.SITE_ORIGIN;}
module.exports={validate,offers,ready};
